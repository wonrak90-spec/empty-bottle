name: V56 Operations Tests

on:
  pull_request:
    paths:
      - 'v56-operations.js'
      - 'v56-reporting.js'
      - 'v56-production-collab.js'
      - 'V56_PRODUCTION_COLLAB.gs'
      - 'V56_PRODUCTION_COLLAB_ROUTER.gs'
      - 'tests/v56-operations.test.js'
      - 'tests/v56-reporting.test.js'
      - 'tests/v56-production-collab.test.js'
      - 'V56_OPERATIONS_PLAN.md'
      - 'config.js'
      - 'sw.js'
      - '.github/workflows/v56-operations-tests.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Guard frozen V54 core
        shell: bash
        run: |
          git fetch origin main
          changed="$(git diff --name-only origin/main...HEAD)"
          echo "$changed"
          bad=""
          while IFS= read -r file; do
            [ -z "$file" ] && continue
            case "$file" in
              v56-operations.js|v56-reporting.js|v56-production-collab.js|V56_PRODUCTION_COLLAB.gs|V56_PRODUCTION_COLLAB_ROUTER.gs|V56_OPERATIONS_PLAN.md|config.js|sw.js|tests/v56-operations.test.js|tests/v56-reporting.test.js|tests/v56-production-collab.test.js|.github/workflows/v56-operations-tests.yml) ;;
              *) bad="${bad}${file}"$'\n' ;;
            esac
          done <<< "$changed"
          if [ -n "$bad" ]; then
            echo "Frozen V54 core file changed:"
            printf "%s" "$bad"
            exit 1
          fi

      - name: Run V56 operations regression tests
        run: node tests/v56-operations.test.js

      - name: Run V56 reporting regression tests
        run: node tests/v56-reporting.test.js

      - name: Run V56 production collaboration tests
        run: node tests/v56-production-collab.test.js

      - name: Static syntax checks
        run: |
          node --check v56-operations.js
          node --check v56-reporting.js
          node --check v56-production-collab.js
          node --check config.js
          node --check sw.js
          cp V56_PRODUCTION_COLLAB.gs /tmp/v56-production-collab.js
          cp V56_PRODUCTION_COLLAB_ROUTER.gs /tmp/v56-production-collab-router.js
          node --check /tmp/v56-production-collab.js
          node --check /tmp/v56-production-collab-router.js
