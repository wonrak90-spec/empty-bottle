# V56 운영성 개선 계획

Base: `main@5e8bd0a1fe95dd26d6c281dcc17c9a40063a6999`
Branch: `feature/v56-operations-usability`

## 1. 조회/출력 - 관리 수정 모드 잔존 데이터

### 확인된 원인
- 검색/최근목록 재조회 시 `v22EditCard`를 닫거나 초기화하지 않음.
- 다른 기록 상세를 열 때 기존 수정 폼을 닫지 않음.
- 조회 탭을 벗어날 때 `currentViewRecord`와 수정 폼 상태가 남음.
- V22의 취소 버튼은 inline `$('v22EditCard')...` 형식이라 전역 `$`가 없는 환경에서는 동작이 불안정할 수 있음.

### V56 처리
- 검색/목록 갱신 전 수정 폼 숨김 + 입력값 초기화 + 상세 Record context 해제.
- 다른 기록 상세 오픈 전 이전 수정 폼 초기화.
- 조회 탭 이탈 시 상세/수정 context 초기화.
- 취소 버튼을 `V56Operations.cancelEdit()`에 직접 연결.
- 관리자/작업자 권한 로직과 Audit 저장 방식은 변경하지 않음.

## 2. 생산제품 연동 - 현장 운영 단순화

### 현재 흐름
1. 제품명/제조번호/작업자 입력
2. 동일 제품/제조번호의 공동 작업 세션 탐색 및 자동 합류
3. 5초 주기 공동세션 동기화
4. WMS/바코드 스캔 즉시 서버 저장
5. 공동 세션 전체 종료

기능적으로는 다중 작업자 보호가 되지만 현장 작업자가 '공동 세션' 개념까지 이해해야 하므로 복잡도가 높음.

### V56 화면 목표
1. 제품명 + 제조번호 입력
2. 작업 시작
3. WMS 라벨 스캔
4. 등록 목록/파레트 수량 확인
5. 작업 완료

### V56 1차 적용
- 로그인 사용자는 등록자 입력칸을 숨기고 로그인 사용자로 자동 유지.
- '공동 작업 세션' 안내는 화면에서 숨김. 서버의 중복방지/동기화 로직은 유지.
- 작업 중 화면에 `① WMS 라벨 스캔 → ② 등록 목록 확인 → ③ 작업 완료` 안내.
- WMS 라벨 스캔을 기본 버튼으로, 바코드 스캔은 보조 수단으로 표현.
- 직접 검색은 '스캔이 안 될 때만 사용'하는 보조 기능으로 명확화.
- '작업 종료'를 '작업 완료'로 변경.
- 저장 API/생산 세션 API 자체는 변경하지 않음.

## 3. 추후 검토
실제 현장 사용 후에도 즉시저장 방식이 불편하면 V56 2차에서
`스캔 임시목록 → 최종확인 → 일괄 확정` 구조를 검토한다.
이 단계는 운영 Apps Script의 생산 API 수정이 필요하므로 별도 검증 후 진행한다.

## 안전 원칙
- V55 OCR Learning Store와 별도 브랜치.
- main 미변경.
- 기존 입고 OCR/저장/조회/출력 데이터 구조 미변경.
- 운영 Apps Script 미변경.


## 4. 조회/출력 - 생산연동 및 Summary Report

### 개별 조회 상세
- 입고검수 Record를 열면 기존 `trace` API로 생산 투입 이력을 확인.
- 연동된 생산제품 / 제조번호 / WMS P.No / 수량을 조회 상세 하단에 표시.
- 아직 생산 투입 전이면 `생산 투입 기록 없음`으로 명확히 표시.

### 클레임·일탈용 생산/파레트 Summary
다중 선택한 검수기록 기준으로:
- 생산제품 / 제조번호별 Summary
- 연동 Pallet 수 / 투입수량
- 공병 품명
- 생산 투입 Pallet List
- 원 입고검수 기록
- 생산 미연동 검수기록 수
를 한 번에 출력한다.

목적:
- 클레임, 일탈, 공급업체 이슈 발생 시 대상 공병이 어떤 생산제품/Lot에 사용되었는지 신속 추적.
- 반대로 특정 생산제품/Lot에 어떤 입고 공병이 투입되었는지 확인.

### 일일 검수 Summary
검수 담당자가 조회목록에서 당일 기록을 다중 선택한 후 팀장 보고용 Summary를 출력한다.

표시 항목:
- 검수 건수
- 총 Pallet 수
- 총 검수수량
- 품목 수
- 적합 / 확인필요 건수
- 품목별 입고건 / Pallet / 검수수량 / 확인필요
- 특이사항 / 확인 필요 건
- 검수 상세
- 검수 담당 / 팀장 확인 서명란

사진은 기본 제외하여 1장 요약을 우선하고, 상세 증빙은 기존 기록서/사진 포함 출력 기능을 사용한다.

### 구현 원칙
- 신규 Backend schema 없음.
- 기존 `trace`, `getRecord`, `batchGetRecords` API 재사용.
- 클레임/일탈 Summary와 일일 Summary 모두 선택된 검수 Record ID를 기준으로 생성.
- 생산 연동은 `ProductionPallets.검수기록ID`를 우선 사용하여 검수 기록과 연결.
